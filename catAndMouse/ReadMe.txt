The function computes the absolute distance of each 
cat (A and B) from the mouse's position (z), compares the distances,
and returns the name of the closer cat—or "Mouse C" if both cats are 
equally close and the mouse escapes.
