The function checks each number between 
the largest in array `a` and the smallest in array `b`, 
and counts how many of those numbers are divisible by every element in `a` 
and also divide every element in `b`.
