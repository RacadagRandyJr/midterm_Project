The function loops through each character in the string, 
and if it's a letter, it shifts it forward by k positions 
(wrapping around the alphabet if needed), while leaving non-letter 
characters unchanged, then returns the fully encrypted string.
