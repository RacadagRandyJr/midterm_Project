The function compares the absolute difference in ASCII values 
between each pair of adjacent characters in the original string 
and its reversed version, and if all differences match, it returns 
"Funny"; otherwise, it returns "Not Funny".
